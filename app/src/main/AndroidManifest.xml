<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.srb.practical_assignment_3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".update_question_by_spinner"></activity>
        <activity android:name=".update_question" />
        <activity android:name=".show_questions" />
        <activity android:name=".insert_question" />
        <activity android:name=".exam_screen" />
        <activity android:name=".dashboard" />
        <activity android:name=".quiz_type_selection" />
        <activity android:name=".registration" />
        <activity android:name=".login" />
        <activity android:name=".splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>